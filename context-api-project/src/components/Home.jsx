import { useContext } from "react";
import UserContext from "../context/userContext";

function Home() {
    // const {user} = useContext(UserContext);
    // if(!user) return <p>Please login to see the content</p>                                                                                                               
  return (
    // <>Welcome: {user.username}</>
    <>Welcome</>
  )
}

export default Home